import { makeStyles } from '@mui/styles';

export default makeStyles(() => ({
  image: {
    width: '100%',
    height: '400px',
    objectFit: 'cover',
    borderRadius: '5px',
    marginBottom: '20px',
  },
}));
